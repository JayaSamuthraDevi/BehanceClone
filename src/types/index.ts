export type BasicButtonProps = {
  clickFunction?: any;
  label?: string;
  disabled?: boolean;
  sub_className?: string;
};

export type CardDataType = {
  icon: React.ElementType;
  text: string;
};

export type ProjectDataType = {
  img: any;
  title: string;
  category: string;
};
